# Optional higher-sampling overlay for a dedicated structure-confidence rerun.
# The normal launcher already runs the bounded default from config/config.yaml.
structure_prediction:
  enabled: true
  methods: ["boltz2", "chai"]
  diffusion_samples: 5

analysis:
  plots:
    fig2_structure_confidence:
      enabled: true
