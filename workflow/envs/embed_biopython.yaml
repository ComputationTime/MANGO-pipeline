name: mango-embed-biopython
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.11
  # Keep saved tensor files on the same tested ABI as the training runtime.
  - pytorch::pytorch=2.5.1
  - pytorch::cpuonly
  - numpy
  - biopython>=1.81
