name: mango-embed-esm2
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pytorch=2.5.1
  - pytorch-cuda=12.1
  - cuda-version=12.1
  - numpy
  - pip
  - pip:
      # ESM2 weights auto-download to ~/.cache/torch/hub on first use.
      - fair-esm==2.0.0
